# secretary.config.yml は commit 対象。秘匿値（API キーやカレンダー ID 等）は
# 入れず、呼び名や境界時刻のような非秘匿な設定だけを置く前提。
# CI（GitHub Actions）でも checkout して読むので、コミットしないと参照できない。

# Claude Code 用のリポジトリ内メモ（公開しない）
CLAUDE.md

# 環境変数（NTFY_TOPIC など）
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
